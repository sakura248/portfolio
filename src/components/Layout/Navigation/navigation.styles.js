import styled from "styled-components";

// type OpenProps = {
//   isOpen?: boolean,
// };

export const DesktopMenu = styled.div`
  list-style: none;
  text-align: right;
  margin-right: 15px;

  a {
    color: #000000;
    font-size: 1.25rem;
    font-weight: 700;
    text-decoration: none;
  }

  a:hover {
    color:#ff49a1;
  }

  }
`;
